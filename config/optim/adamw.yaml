name: adamw
lr: 0.001
betas: [0.9, 0.95]
weight_decay: 0.05
# layer_decay: 0.8  # for layer-wise learning rate decay (1.0 = no decay)
# skip_list: []     # list of parameter names to skip weight decay
# no_lr_scale_list: []  # list of parameter names to skip LR scaling
