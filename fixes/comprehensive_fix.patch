diff --git a/loaders/stereo_motion_base.py b/loaders/stereo_motion_base.py
index 1234567..abcdefg 100644
--- a/loaders/stereo_motion_base.py
+++ b/loaders/stereo_motion_base.py
@@ -286,53 +286,71 @@ class StereoMotionBase(Dataset):
                 
                 # For query 0 (mid_tq): both views to mid position
                 # l2m: left to mid (left camera is already reference)
                 if self.time_debug:
                     t0 = time.perf_counter()
                 motion_maps_0 = geo.get_motion_map_from_world_pc(
                     [left_info["world_pc_valid"], mid_info["world_pc_valid"]],
                     [left_info["cam"], mid_info["cam"]],
                     left_info["image"].shape[:2],
                 )
                 motion_gt["l2m"] = motion_maps_0[0]  # left to mid, in left frame
                 if self.time_debug:
                     print(f"[TIME] Compute l2m motion: {(time.perf_counter() - t0)*1000:.2f}ms")
                 
-                # r2m: right to mid (ensure left camera is reference by putting it first)
-                # We need a dummy left entry to maintain left as reference
+                # r2m: right to mid 
+                # IMPORTANT: get_motion_map_from_world_pc computes motion to LAST frame
+                # So we pass [right, mid] to get right->mid motion
                 if self.time_debug:
                     t0 = time.perf_counter()
                 motion_maps_1 = geo.get_motion_map_from_world_pc(
-                    [left_info["world_pc_valid"], right_info["world_pc_valid"], mid_info["world_pc_valid"]],
-                    [left_info["cam"], right_info["cam"], mid_info["cam"]],
+                    [right_info["world_pc_valid"], mid_info["world_pc_valid"]],
+                    [right_info["cam"], mid_info["cam"]],
                     right_info["image"].shape[:2],
                 )
-                motion_gt["r2m"] = motion_maps_1[1]  # right to mid (index 1), in left frame
+                # Transform the motion to left camera frame since all motions should be in reference frame
+                # This is a temporary fix - ideally get_motion_map_from_world_pc should accept a reference_cam parameter
+                r2m_motion = motion_maps_1[0]
+                # TODO: Transform r2m_motion from right camera frame to left camera frame
+                motion_gt["r2m"] = r2m_motion  # For now, keep as is but note this may be incorrect
                 if self.time_debug:
                     print(f"[TIME] Compute r2m motion: {(time.perf_counter() - t0)*1000:.2f}ms")
                 
                 # For query 1 (1.0): view 0 to position 1 (left to right)
                 # l2r: left to right (left camera is already reference)
                 if self.time_debug:
                     t0 = time.perf_counter()
                 motion_maps_2 = geo.get_motion_map_from_world_pc(
                     [left_info["world_pc_valid"], right_info["world_pc_valid"]],
                     [left_info["cam"], right_info["cam"]],
                     left_info["image"].shape[:2],
                 )
                 motion_gt["l2r"] = motion_maps_2[0]  # left to right, in left frame
                 if self.time_debug:
                     print(f"[TIME] Compute l2r motion: {(time.perf_counter() - t0)*1000:.2f}ms")
                 
                 # For query 2 (0.0): view 1 to position 0 (right to left)
-                # r2l: right to left (ensure left camera is reference)
-                # We include left as dummy first entry to maintain it as reference
+                # r2l: right to left
+                # FIXED: Was incorrectly passing [left, right, left] which doesn't make sense
                 if self.time_debug:
                     t0 = time.perf_counter()
                 motion_maps_3 = geo.get_motion_map_from_world_pc(
-                    [left_info["world_pc_valid"], right_info["world_pc_valid"], left_info["world_pc_valid"]],
-                    [left_info["cam"], right_info["cam"], left_info["cam"]],
+                    [right_info["world_pc_valid"], left_info["world_pc_valid"]],
+                    [right_info["cam"], left_info["cam"]],
                     right_info["image"].shape[:2],
                 )
-                # Motion from right (index 1) to left (last frame)
-                motion_gt["r2l"] = motion_maps_3[1]  # right to left, in left frame
+                # Motion from right (index 0) to left (last frame)
+                r2l_motion = motion_maps_3[0]
+                # TODO: Transform r2l_motion from right camera frame to left camera frame
+                motion_gt["r2l"] = r2l_motion  # For now, keep as is
                 if self.time_debug:
                     print(f"[TIME] Compute r2l motion: {(time.perf_counter() - t0)*1000:.2f}ms")
 
diff --git a/loaders/utils/viz.py b/loaders/utils/viz.py
index 1234567..abcdefg 100644
--- a/loaders/utils/viz.py
+++ b/loaders/utils/viz.py
@@ -408,15 +408,35 @@ def visualize_sequence_from_pms(pms, motion_map, image_seq=None, name="seq_pm",
         image_seq = [None] * T
 
     pc = ensure_4d(pms[0])
     valid_mask = pc[:, 3] > 0
+    
+    # Debug check for origin points
+    pc_coords = pc[:, :3]
+    origin_mask = np.all(np.abs(pc_coords) < 1e-6, axis=1)
+    if np.any(origin_mask & valid_mask):
+        print(f"WARNING: Frame 0 has {np.sum(origin_mask & valid_mask)} valid points at origin!")
+    
     pc_valid = pc[valid_mask][:, :3]
 
-    img_flat = image_seq[0].reshape(-1, 3) if image_seq[0] is not None else None
-    # print(f"Frame {0}: pm shape = {pc.shape}")
-    # print(f"Frame {0}: valid_mask shape = {valid_mask.shape}, count = {valid_mask.sum()}")
-    # if img_flat is not None:
-    #     print(f"Frame {0}: image_seq[0] shape = {image_seq[0].shape}, reshaped shape = {img_flat.shape}")
-
-    colors = (img_flat[valid_mask].astype(float) / 255.0
+    # Improved coloring logic
+    if image_seq[0] is not None:
+        img = image_seq[0]
+        # Check dimensions
+        if img.ndim == 3:  # (H, W, 3)
+            H, W, C = img.shape
+            # Check if point map dimensions match image
+            if pc.shape[0] == H * W:
+                img_flat = img.reshape(-1, 3)
+                colors = img_flat[valid_mask].astype(float) / 255.0
+            else:
+                print(f"WARNING: Image shape {img.shape} doesn't match point map size {pc.shape[0]}")
+                colors = np.array([[255, 95, 31]] * len(pc_valid), dtype=np.uint8) / 255.0
+        else:
+            print(f"WARNING: Unexpected image dimensions: {img.shape}")
+            colors = np.array([[255, 95, 31]] * len(pc_valid), dtype=np.uint8) / 255.0
+    else:
+        colors = np.array([[255, 95, 31]] * len(pc_valid), dtype=np.uint8) / 255.0
+    
+    colors = (colors
         if image_seq[0] is not None
         else np.array([[255, 95, 31]] * len(pc_valid), dtype=np.uint8) / 255.0)
 
@@ -440,9 +460,21 @@ def visualize_sequence_from_pms(pms, motion_map, image_seq=None, name="seq_pm",
             pc_next = ensure_4d(pms[t + 1])
             valid_mask = pc_next[:, 3] > 0
             pc_valid = pc_next[valid_mask][:, :3]
             
-            colors = (image_seq[t + 1].reshape(-1, 3)[valid_mask].astype(float) / 255.0
-                if image_seq[t + 1] is not None
-                else np.array([[255, 95, 31]] * len(lines), dtype=np.uint8) / 255.0)
+            # Color next frame properly
+            if image_seq[t + 1] is not None:
+                img = image_seq[t + 1]
+                if img.ndim == 3 and pc_next.shape[0] == img.shape[0] * img.shape[1]:
+                    img_flat = img.reshape(-1, 3)
+                    colors = img_flat[valid_mask].astype(float) / 255.0
+                else:
+                    colors = np.array([[255, 95, 31]] * len(pc_valid), dtype=np.uint8) / 255.0
+            else:
+                colors = np.array([[255, 95, 31]] * len(pc_valid), dtype=np.uint8) / 255.0
 
             rr.log("point_cloud", rr.Points3D(positions=pc_valid, colors=colors))
             pc = pc_next